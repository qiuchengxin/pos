<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"        "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><mapper namespace="com.market.pos.mapper.TargetMapper">    <resultMap id="TargetResultMap" type="com.market.pos.pojo.TargetType">        <id property="id" column="id"/>        <result property="userid" column="userid"/>        <result property="target" column="target"/>        <result property="type" column="type"/>    </resultMap>    <select id="findTypeByTarget" resultType="String">        select type from target_type where target = #{target} order by id desc limit 1;    </select>    <insert id="insertUserIdAndTarget">        insert into target_type (userid, target, type) values (#{userid},#{target},#{type});    </insert>    <update id="updateByUserId">        update target_type set type = #{type} where userid = #{userid} order by id desc limit 1;    </update>    <delete id="delByUserId">        delete from target_type where userid = #{userid};    </delete></mapper>